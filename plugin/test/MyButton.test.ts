import { describe, it, expect } from "vitest";

describe("Test markdown authoring with my-button.md fixture", () => {
  it.todo("setting playground to true injects CodePlayground wrapper");

  it.todo("CodePlayground gets code tokenized code snipped in default slot");

  it.todo("CodePlayground gets source code as a VueJS component in the 'code' parameter");
});
